/**
 * Re-export from the unified elements module.
 * This module is kept for backwards compatibility.
 *
 * @see elements.ts for the complete element database
 */
export { ELEMENT_SYMBOLS as VALID_ELEMENTS, isValidElement } from './elements';
